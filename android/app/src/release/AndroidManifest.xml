<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Public collector releases require HTTPS. Debug builds may still use
         cleartext HTTP for local LAN development via the debug manifest. -->
</manifest>
